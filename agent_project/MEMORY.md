# 🧠 项目记忆

> 自动加载到 Reasonix 系统提示中的持久记忆索引。
> 使用 `remember` 工具添加条目，不要手动编辑此文件。
>
> 格式：`- <type> :: <scope> :: <name> — <description> [<priority>]`

<!-- MEMORY_INDEX -->
_（此处自动填充记忆条目，运行 `/skill remember` 或使用 `remember` 工具后可见）_
<!-- /MEMORY_INDEX -->